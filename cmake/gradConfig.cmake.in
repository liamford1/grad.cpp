@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Threads)
if(NOT APPLE)
    # grad::core links BLAS::BLAS on non-Apple platforms.
    find_dependency(BLAS)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/gradTargets.cmake")
check_required_components(grad)
